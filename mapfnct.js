const friends = ['Alice', 'Bob', 'Charlie'];

const pizzaOrders = friends.map(friend => `${friend}'s Pizza`);
console.log(pizzaOrders);


// Output: ["Alice's Pizza", "Bob's Pizza", "Charlie's Pizza"]
